# Tasks Planned

No tasks planned. v1 implementation complete pending validation against live AWS/Belvedere environment.

Potential follow-ups (not scheduled):

- Code-split Leaflet/AWS SDK to reduce bundle size
- Component tests for CredentialGate and LandingPage with mocked AWS
- CI workflow (GitHub Actions) for test + build on push
